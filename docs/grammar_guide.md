# Plix Grammar & Coding Guide

## Basic Syntax
```
func name(params) -> return_type { ... }
struct Name { field: type }
impl Name { ... }
```

## Types
- `int`, `float`, `str`, `bool`, `any`
- Generics: `<T, U>`

## Examples
See `examples/*.px`
