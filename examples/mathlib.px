// a user module — imported by module_app.px
const GOLDEN = 1.618;

func square(x) {
    return x * x;
}

func cube(x) {
    return x * x * x;
}

func sumlist(xs) {
    auto t = 0;
    for (x in xs) {
        t = t + x;
    }
    return t;
}
